@using HttpWebServerMvcF.Domain
@model HttpWebServerMvcF.Domain.Party
@{
    ViewBag.Title = $"Party {Model.Title}";
}

<body>
    <p>Приглашаем всех на тематическую вечеринку @Model.Title!</p>

    <form action="/party/vote/@Model.Id" method="POST">
        Имя:<br> <input type="text" name="name"><br><br>
        Иду:<br> <input type="checkbox" name="attend"><br><br>
        Коментарий:<br> <input type="text" name="reason"><br><br>
        <input type="submit" value="Голосовать">
    </form>
    <br>
    <br>
    <a href="/party/participants/@Model.Id">Список гостей.</a>
    <br>
    <br>
    @*<script>
        var xhr = new XMLHttpRequest();
        xhr.open('GET', 'participants.html', false);
        xhr.send();
        if (xhr.status == 200) {
            var test = xhr.responseText.replace('<a href="index.html">Вернуться.</a>', "<br>");
            document.write(test);
        }
    </script>*@
</body>